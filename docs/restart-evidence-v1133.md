# Semantic freshness and restart evidence

The earlier v1.13.2 production incident remains **UNCLASSIFIED SEMANTIC INPUT
CHANGE — INSUFFICIENT RETAINED EVIDENCE**. The independent normalization defect
does not establish its historical cause.

## Confidence contract

Normalization 1.1 uses the existing family-specific freshness policy. Identical
provider evidence within one material state yields identical confidence. Market
boundaries remain 36, 72 and 168 hours; crossing them may legitimately change
confidence. Missing/malformed timestamps and the existing `stale` consumer label
retain their established behavior. New provider values remain material.

## Capture operation

Run `python -m tools.validation.capture_restart_evidence --output <private-file>`
inside the existing production environment, using its retained inspection token.
Never transfer the token. The destination is restricted to the configured HTTPS
public origin, redirects are disabled, and ordinary TLS verification is required.

Establish an idle, memory-safe boundary before invoking the diagnostic process:
no synchronization, browser capture, market construction or other heavy phase.
Keep the existing memory reserve. Observe active/requested semantic state over a
bounded window before restart; this command does not restart or schedule work.

The inspection principal is synthetic and explicitly distinct from the user's
account. Capturing its selected league is not a substitute for real-account
direct/switch/concurrency acceptance. The latter remains independently required.

The collector pages retained provider inputs, canonical valuation rows, ordered
market rows and projection records, and reads each canonical Brain asset. Only
the documented missing-Brain-asset response represents absent evidence; unknown
HTTP failures are errors. Required semantic digests must be complete. The provider
digest is recomputed with the existing canonical implementation and must equal
the published market digest. Before/after fences reject moving captures.

Provider source timestamps, numerical confidence inputs, normalization scale and
freshness states are retained safely. Other leaf values and private identifiers
are fingerprinted. Structural ordering and every unknown field remain compared.
Hierarchical evidence stores each parent path once rather than repeating it at
every leaf. Compact key references retain full SHA-256 mappings and reject collisions.
An operational provider timestamp is not mislabeled as a global market generation.

Files are private diagnostic evidence, **not public release assets**. They use
atomic owner-only publication, a 64 MiB output ceiling and 500,000 structural-node
limit. Input responses and total input also have explicit byte limits. A bounded
failure journal excludes exception text, request URLs, credentials and raw bodies.
Moving-boundary and provider/publication mismatches are persisted before rejection.
Use `--compare <before-file>` after restart; incomplete baselines cannot qualify.

Unchanged semantic evidence requires compatible artifact load and no fresh
construction. Changed evidence must be classified from the retained structural
differences, allowed to settle, and assessed at a new stable boundary. Never force
compatibility or silently exclude new differing fields. Remove private captures
after reconciliation under the existing cleanup policy.

## Isolation review

The v1.13.1 boundary matrix remains authoritative for account/session chrome,
league runtimes, franchise evidence, historical series and intentionally global
NFL/provider/market-memory data. This correction preserves those boundaries.

Reviewed consumers include Home, My Team/Team HQ, League/rankings, matchups,
history/transactions/picks, FOIS/Front Office, Market/player details, Trade Center,
Create/Trade For/Shop/Recommended Trades, valuation/calibration, and account/settings.
The app injects selected data/runtime proxies or explicit league resolvers;
authenticated middleware replaces caller league/manager selectors and denies
foreign league paths. Private responses remain `no-store, private`. Crawl cache
keys include selected league; history-section keys include league and season;
Home generation includes league; shared rendered evidence never supplies session
chrome. Projection audit reference fixtures are explicitly labeled static, not
current-league truth. The legacy `day_traders_free_agent` filter token remains a
compatibility identifier, not a source of ownership or a displayed league name.

Existing regressions cover two accounts, colliding roster/player IDs, direct and
repeated switches, concurrent requests, independent runtime maintenance, global
positive controls and 500 memberships without eager runtime hydration. Production
acceptance must separately exercise the actual authorized leagues after deployment.
