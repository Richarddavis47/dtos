name: Temporary v1.8.6 asynchronous market cgroup gate

on:
  workflow_dispatch:
  push:
    branches:
      - dtos-v1.8.6-async-market-generation

permissions:
  contents: read

concurrency:
  group: v1.8.6-market-cgroup-${{ github.ref }}
  cancel-in-progress: false

jobs:
  market-memory:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Build production Python 3.12 image
        run: >-
          docker build --file tools/validation/Dockerfile.market-cgroup
          --tag dtos-market-cgroup:${{ github.sha }} .
      - name: Generate sanitized two-million-version fixture
        run: |
          docker volume create dtos-market-fixture
          docker run --rm \
            --mount source=dtos-market-fixture,target=/fixture \
            --env DTOS_FIXTURE_ROOT=/fixture \
            dtos-market-cgroup:${{ github.sha }} \
            python -m tools.validation.generate_sanitized_identity_fixture
      - name: Run asynchronous market lifecycle under 2 GiB cgroup
        run: |
          mkdir -p benchmark-output
          docker run --rm \
            --name dtos-v186-market-cgroup \
            --memory 2g --memory-swap 2g --cpus 2 --pids-limit 512 \
            --mount source=dtos-market-fixture,target=/fixture \
            --mount type=bind,source="${PWD}/benchmark-output",target=/output \
            --env DTOS_FIXTURE_ROOT=/fixture \
            --env DTOS_BENCHMARK_OUTPUT=/output/summary.json \
            --env DTOS_CACHE_FILE=/fixture/dtos_cache.json \
            --env DTOS_HISTORY_DB_FILE=/fixture/dtos_history.sqlite3 \
            --env DTOS_HISTORY_STORAGE_ROOT=/fixture \
            --env DTOS_DURABLE_HISTORY_REQUIRED=true \
            --env SLEEPER_LEAGUE_ID=validation-league-1804 \
            --env DTOS_BACKGROUND_START_DELAY=3600 \
            --env DTOS_LOG_FORMAT=json \
            dtos-market-cgroup:${{ github.sha }} \
            python -m tools.validation.linux_market_cgroup_gate
      - name: Validate sanitized evidence
        if: always()
        run: |
          test -f benchmark-output/summary.json
          python - <<'PY'
          import json
          from pathlib import Path
          result = json.loads(Path('benchmark-output/summary.json').read_text())
          assert result['passed'] is True
          assert result['memory_max'] == 2 * 1024**3
          assert result['memory_peak'] < int(1.5 * 1024**3)
          assert result['margin_below_limit'] >= 500 * 1024**2
          cold = result['phases']['cold_market']
          responsive = cold['responsiveness']
          assert responsive['warming_client_max_ms'] < 500
          assert responsive['warming_server_max_ms'] < 50
          assert responsive['health_max_ms'] < 500
          assert responsive['liveness_max_ms'] < 500
          assert cold['build_count'] == 1
          text = json.dumps(result).casefold()
          assert not any(value in text for value in (
              '/home/runner', '/fixture/', 'localhost', '127.0.0.1', 'token'
          ))
          print(json.dumps(result, indent=2, sort_keys=True))
          PY
      - name: Upload sanitized summary only
        if: always() && hashFiles('benchmark-output/summary.json') != ''
        uses: actions/upload-artifact@v4
        with:
          name: v1.8.6-linux-market-cgroup-${{ github.sha }}
          path: benchmark-output/summary.json
          if-no-files-found: error
          retention-days: 30
      - name: Remove fixture
        if: always()
        run: docker volume rm -f dtos-market-fixture || true
